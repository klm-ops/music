# 收音机界面视觉对比报告

## 参考来源

- 参考路径：`C:\Users\16696\Downloads\FM\lanhu_shensebanshouyinjifmchushizhuangtai`
- 主要参考文件：`index.html`、`index.css`
- 参考画布：`768px × 1024px`
- 已按要求移除/不迁入：参考文件底部 `block_7` 车载控制栏组件。

## 对比结果

| 项目 | 参考文件 | 当前实现 |
| --- | --- | --- |
| 背景 | `rgba(14,14,14,1)` 深黑 | 收音机激活时强制纯黑 `#000`，禁用纹理背景 |
| 布局 | 单屏纵向居中，主要内容集中在 768px 画布内 | `.radio-stage` 以 768px 参考宽度居中，按视口高度自适应 |
| FM/AM 切换 | 276px × 48px，FM 白底激活，AM 灰字 | 保持同尺寸模型和激活态配色 |
| 频率显示 | 居中大号数字，右侧 ST/MHz | 居中频率，右侧显示信号级别和单位 |
| 频率刻度 | 87-108 范围，中央白色指针 | FM/AM 自动切换范围，纯色刻度与指针 |
| 电台列表 | 底部区域显示“电台列表/暂无收藏” | 底部电台列表，支持全部/收藏模式和当前项高亮 |

## 设计约束处理

- 已移除收音机模块内的渐变卡片、阴影和纹理。
- 收音机模块不复用蓝牙/本地音乐的卡片层级视觉。
- 触控按钮高度不低于 44px，满足移动端触控要求。
